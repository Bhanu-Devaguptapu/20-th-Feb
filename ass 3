#include<stdio.h>
int main(){
	int n,i=1,c=0;
	printf("enter the number:");
	scanf("%d",&n);
	while(i<=n){
		if(n%i==0)
		c++;
		i++;
		}
		if(c==2)
		printf("It is a prime number");
		else
		printf("It is not a prime number");
	return 0;
}
